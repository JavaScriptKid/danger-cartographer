import React from 'react'
import { connect } from 'react-redux'

@connect((state, props) => {
    return {
    }
})

class App extends React.Component {

    render() {
        return (
           <div>
                APP!
           </div>
        );
    }
}

export default App;